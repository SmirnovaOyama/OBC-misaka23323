{
  "name": "openbiocard-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter openbiocard-frontend dev",
    "build": "pnpm -r build",
    "deploy": "pnpm --filter openbiocard-backend run deploy",
    "wrangler": "wrangler"
  },
  "devDependencies": {
    "wrangler": "^4.55.0"
  }
}